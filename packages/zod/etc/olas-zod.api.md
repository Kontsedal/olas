## API Report File for "@kontsedal/olas-zod"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Ctx } from '@kontsedal/olas-core';
import { DeepPartial } from '@kontsedal/olas-core';
import { Field } from '@kontsedal/olas-core';
import { FieldArray } from '@kontsedal/olas-core';
import { Form } from '@kontsedal/olas-core';
import { Validator } from '@kontsedal/olas-core';
import { z } from 'zod';

// @public
export function createZodForm<T extends z.ZodObject<z.ZodRawShape>>(ctx: Ctx, schema: T, options?: ZodFormOptions<T>): Form<{ [K in keyof T['shape']]: ZodToLeaf<T['shape'][K]>; }>;

// @public
export type ExtraValidators = Record<string, Validator<any>>;

// @public
export function rootOnlyZodValidator<T>(schema: z.ZodType<T>): Validator<T>;

// @public
export type UnwrapZod<S> = S extends z.ZodDefault<infer Inner> ? UnwrapZod<Inner> : S extends z.ZodOptional<infer Inner> ? UnwrapZod<Inner> : S extends z.ZodNullable<infer Inner> ? UnwrapZod<Inner> : S;

// @public
export type ZodFormOptions<T extends z.ZodObject<z.ZodRawShape>> = {
    initial?: DeepPartial<z.infer<T>> | (() => DeepPartial<z.infer<T>> | undefined);
    resetOnInitialChange?: 'when-clean' | 'never' | 'always';
    extraValidators?: ExtraValidators;
};

// @public
export type ZodToLeaf<S> = UnwrapZod<S> extends z.ZodObject<infer RawShape> ? Form<{ [K in keyof RawShape]: ZodToLeaf<RawShape[K]>; }> : UnwrapZod<S> extends z.ZodArray<infer Element> ? FieldArray<ZodToLeaf<Element> extends Form<any> | Field<any> ? ZodToLeaf<Element> : never> : Field<z.infer<UnwrapZod<S> & z.ZodType>>;

// @public
export function zodValidator<T>(schema: z.ZodType<T>): Validator<T>;

// @public
export function zodValidatorAsync<T>(schema: z.ZodType<T>): Validator<T>;

// (No @packageDocumentation comment for this package)

```
